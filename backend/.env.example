# Google Colab / AI Studio API 金鑰
COLAB_GOOGLE_API_KEY=

# Google 服務帳號金鑰檔案路徑 (擇一設定)
# 例如：/path/to/your/service-account-file.json
GOOGLE_APPLICATION_CREDENTIALS=

# Google 服務帳號金鑰 JSON 內容 (擇一設定)
# 直接貼上 JSON 內容，確保格式正確。在某些環境下，例如 Docker secrets 或 CI/CD 環境變數，這可能更方便。
GOOGLE_SERVICE_ACCOUNT_JSON_CONTENT=

# Google Drive 輸入資料夾 ID (必要)
# 用於存放等待 AI 處理的原始週報檔案。
WOLF_IN_FOLDER_ID=

# Google Drive 已處理資料夾 ID (必要)
# 用於存放 AI 處理完成後的週報檔案。
WOLF_PROCESSED_FOLDER_ID=

# 報告資料庫的完整路徑 (可選)
# 若不提供，將使用 backend/data/reports.sqlite。 (註: 實際預設路徑可能依 main.py 實作而定)
# 例如：/app/data/reports.sqlite
REPORTS_DB_PATH=

# 提示詞資料庫的完整路徑 (可選)
# 若不提供，將使用 backend/data/prompts.sqlite。 (註: 實際預設路徑可能依 main.py 實作而定)
# 例如：/app/data/prompts.sqlite
PROMPTS_DB_PATH=

# 報告資料庫檔案名 (可選，若 REPORTS_DB_PATH 未指定目錄/為空，則檔案會建立在預設資料目錄下)
# 預設: 'reports.sqlite'
REPORTS_DB_FILENAME=reports.sqlite

# 提示詞資料庫檔案名 (可選，若 PROMPTS_DB_PATH 未指定目錄/為空，則檔案會建立在預設資料目錄下)
# 預設: 'prompts.sqlite'
PROMPTS_DB_FILENAME=prompts.sqlite

# 排程器執行間隔分鐘數 (可選)
# 預設: '15'
SCHEDULER_INTERVAL_MINUTES=15

# 應用程式的公開基礎 URL (可選)
# 用於日誌記錄或未來可能需要生成絕對路徑的場景。
# 例如：https://your-app-domain.com
APP_BASE_URL=
